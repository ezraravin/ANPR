import cv2

# LOAD YOLO MODEL
def Load_YOLO(paramYOLOModelFilePath):
    net = cv2.dnn.readNetFromONNX(paramYOLOModelFilePath)
    net.setPreferableBackend(cv2.dnn.DNN_BACKEND_OPENCV)
    net.setPreferableTarget(cv2.dnn.DNN_TARGET_CPU)